{
  "name": "@repo/util",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "npx only-allow pnpm",
    "check-types": "tsc --noEmit"
  },
  "exports": {
    "./timer": "./dist/timer-util.js"
  },
  "devDependencies": {
    "@repo/ts-config": "workspace:*",
    "@types/node": "^22.10.4"
  }
}
