type GameSessionPageProps = {
  params: {
    sessionId: string
  }
}

const GameSessionPage = ({ params }: GameSessionPageProps) => {
  return (
    <div>
      GameSessionPage
    </div>
  )
}

export default GameSessionPage
